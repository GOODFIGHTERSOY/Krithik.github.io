# Krithik S — Portfolio

Status: prepared for GitHub Pages; publication has not been verified.

Expected site URL: https://goodfightersoy.github.io/Krithik.github.io/
Resume: [Download PDF](./Krithik-S-Resume.pdf)

## Publish
Upload index.html, Krithik-S-Resume.pdf, .nojekyll and this README to the root of GOODFIGHTERSOY/Krithik.github.io on main. Preserve unrelated repository files. If an existing CNAME points to a previous host, remove it and clear the custom-domain setting.

In Settings > Pages select Deploy from a branch, main, / (root), Save. Leave Custom domain empty.

This is standalone static HTML with inline CSS and JavaScript. The .nojekyll file disables Jekyll; no _config.yml, Jekyll theme, dependencies or build step is needed. No CNAME is needed for the default address.

## Verify before sharing
Wait for the Pages deployment to succeed and use Visit site to confirm its exact URL. Check navigation, projects, resume download, mobile layout and contact email draft. The contact form opens the visitor's email application; it does not send mail through a backend. Check the downloaded PDF's Portfolio link. Then change the status above to Published and record the verified URL.

The included PDF uses the expected URL and is pending live-site verification. Do not represent it as a verified final resume until publication succeeds.

To use https://goodfightersoy.github.io/ without the repository suffix, the repository must instead be named GOODFIGHTERSOY.github.io; update the canonical URL and PDF link if you rename it.
